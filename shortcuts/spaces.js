
/* SPACES */

// (new EventDispatcher()).setHandler('tab', HYPER, () => Space.active().next().active());

// (new EventDispatcher()).setHandler('tab', HYPER_SHIFT, () => Space.active().previous().active());
